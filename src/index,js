# File 3 of 4 — src/index.js

## How to add this file in GitHub:

1. In your repo, click **"Add file"** → **"Create new file"**
2. In the filename box, type: `src/index.js`
3. Copy and paste the code below into the editor
4. Scroll down and click **"Commit changes"** → **"Commit changes"** again

---

```js
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

---

**When done, say "next" for File 4 of 4 — the last one!**
